# Agent Note：公开模型费率与预估花费

Status: implemented

[English](2026-09-10-published-model-rates-and-estimated-spend.md) | 中文

## Problem

harness 的每个用量界面此前只报告 token。读者能看到某一轮消耗了 15.8K token，却看不到这一轮要花多少钱，于是比较两条路由——便宜的 flash 模型与旗舰模型——必须离开产品去手工查价。信息其实已经在进程里：已安装的 pi-ai 目录为其描述的模型标了价，而 `catalog.ts` 读到了那个 `cost` 字段，却因为没有任何消费方报告花费而把它藏在全零哨兵值后面丢弃了。

本部署实际服务的网关路由（CommandCode、OpenCode）在各自的模型端点上根本不发布价格：`/models` 只回答 id、名称和上下文长度，别无其他。两家运营方确实会在自家网站上公布费率表，但那是散文式页面而非机器可读字段，而且它们变化时已安装的目录不会跟随——内置的 pi-ai 目录仍然携带 2026-09-10 之前的 Flash 费率，而过期的目录会把每一轮 Flash 按错误费率计价，而不是拒绝计价。因此「为用量定价」需要两半——让部署能够声明目录没有携带的费率，以及一个把 token 桶折算成金额、并在没有费率时不凭空发明的折叠。

## Decision

**`LlmModelCost` 是公开标价，不是账单记录。** 该类型位于 [types.ts](../../../../packages/llm/llm/src/types.ts)，随 `LlmResolvedModelInfo.cost` 传递，经由 `normalizeModelInfo`（把每个声明的价格校验为有限非负数）贯穿，并由 `buildModelCatalog` 暴露给浏览器上的 `ModelCatalogModel.cost`。它的 JSDoc 写明了使它保持诚实的那条契约：部署实际支付什么属于部署自己的合同，所以这里是供展示的标价；而缺失的桶保持缺失，而不是默认为零——因为「这条路由不对缓存写入收费」与「没有人公布费率」是两件不同的事实，只有前者可以乘进总额。

**会随时段变动的费率由规则声明，而费率在该次尝试自身的时刻上读取。** DeepSeek 公布两个档位：上面这些价格，以及一个在工作日 01:00–04:00 与 06:00–10:00 UTC 内恰好翻倍的峰时档，周末则完全属于谷时——这是费率表自己的措辞，「Peak runs Monday to Friday only, so it is never charged at the weekend」。网关的 `/models` 端点与各运营方机器可读的界面都不携带这条规则，因此由 `LlmModelCost.peak` 以「一个系数加若干时间窗」的形式携带，而不是第二张价格表：费率表公布的就是这个比例，所以修改基础档不可能让两个档描述出不同的费率。`LlmModelCostPeakWindow` 用三字母小写的 `days` 与补零的 `HH:MM` UTC 表示 `start`/`end`——补零是为了让字典序*就是*时钟序，这正是 `detachedPeak` 中的先后检查成为时钟检查的原因。格式错误的档位会在写入处被拒绝：非正系数、没有时间窗、没有星期、无法定位的时刻，或结束时间不晚于开始时间。在 [catalog.ts](../../../../packages/llm/llm-pi-ai/src/catalog.ts) 中的 `declaredPeak` 是配置侧的另一半，它也会把档位校验到*目录*费率之上——时间窗属于路由，而不属于公布那个数字的人。

**模型条目自行声明费率；只声明一半的价对会被拒绝。** [catalog.ts](../../../../packages/llm/llm-pi-ai/src/catalog.ts) 中的 `PiAiModelProfile.cost` 接受 `input`、`output`，以及可选的 `cacheRead` 与 `cacheWrite`——单位为美元/百万 token。`declaredCost` 要求 `input` 与 `output` 同时给出：只给出其一会让另一桶在该模型服务的每个请求上按零计费，而这恰好会在最需要准确的地方低估花费，因此它抛出 `PiAiCatalogError` 而不是接受。声明的费率优先于已安装目录的费率；未声明的条目保留目录的费率。全零的 `NO_COST` 哨兵值仍然表示「没有费率」，`pricedCost` 正是适配器在决定是否报告 `cost` 时区分两者的依据——未定价的模型什么也不暴露，而不是 `$0.00`，因此没有公开费率的路由显示为空，而不是显示为免费。

**花费由同一个折叠估算，并且它拒绝不诚实的总额。** [usage-cost.ts](../../../../packages/llm/token-meter/src/usage-cost.ts) 中的 `estimateUsageCost` 把每个承载 token 的桶乘以其每百万费率，最后统一缩放一次。当某个桶承载 token 而费率并未为其定价时，它返回 `undefined` 而不是部分和——因为一个默默把该桶按零计费的总额比没有总额更糟。`sumUsageCosts` 跨多次读数相加并跳过无法定价的那些，因此一个未定价的请求不会让同一会话中已定价的请求失效。

**花费按每次尝试定价，因为合计不指向任何单一费率。** 当模型在一轮中途更换时，*或*当某一轮运行期间跨过了一个档位边界时，该轮汇总后的桶都无法乘以单一费率——这是两种形状相同却彼此不同的失败，而后者除非估算去读时钟，否则完全不可见。因此 `deriveTurnTokenUsage` 还会公布 `attempts`：每次已计费尝试自己的桶、它的路由，以及 `at`——即结束该次尝试的那个事件的 epoch 毫秒（完成事件本身，或是结束了某次没有完成事件的尝试的重试或步骤结束）。[turn-cost.ts](../../../../packages/client/ui-chat/src/client/chat/turn-cost.ts) 按 `rateBandAt` 为该次尝试自身时刻判定的档位为其定价，再对各金额求和，因此中途换过模型的轮次与跨过边界的轮次都会报告其各请求实际计费的金额。当任何一次尝试的路由未记录或未定价，或某个费率无法为那次尝试承载的某个桶定价时，它仍然什么都不报告：部分和会把缺失的那些尝试按零计价，却依然看起来像是这一轮的花费。结果会携带它所用的档位，因此 Chat 的轮次用量对话框在六位小数的 `Estimated cost (list price)` 之后接着渲染 `(peak)`、`(off-peak)` 或 `(peak and off-peak)`（当这一轮自身的各请求跨过边界时），因为同样 token 却翻倍的金额必须说明原因。Trajectory 账本按每个请求记录的时刻为其定价，把累计值作为 `cumulativeCost` 连同它计费所用的档位一起携带，并在其 Usage 检查器的 `This request` 与 `Session cumulative` 两块下分别显示两半。

**档位只从该次尝试的时刻读取，绝不从渲染时的时钟读取。** 对话记录是一份记录：在周六运行的一轮，不该因为读者在周一打开它而按周一的峰时费率重新计价；一周后重新加载的页面也不该悄悄改写一个月的花费。因此每个定价界面都从结束该次尝试的持久事件取时刻——已完成的请求取 `assistant/message`，Trajectory 账本取该请求自身的 `completedAt`/`startedAt`——在没有记录到时刻时退回基础档，这是低估而非凭空发明的方向。规则本身只存在于一处，即 [rate-schedule.ts](../../../../packages/llm/token-meter/src/rate-schedule.ts)，因此对话记录与账本不可能对同一个请求得出不同结论；时间窗无法定位的每一种情形都是闭合的，也就是说格式错误的规则会按基础档计价而不是抬高每一个估算，而宿主机对这类规则自身的拒绝仍然是这条防线的第一道，而不是唯一一道。

**单一费率的价目表根本不命名档位。** GLM、Kimi 以及这些网关上的其他每条路由都只公布一个从不移动的费率，而「谷时」并不是它的同义词：为这样的路由定价在每个时刻读到的都是同一个费率，但把它的唯一档位标成「谷时」，等于报告运营方从未公布过的区分，还会让读者去找那个它其实并未被减免的峰时费率。因此只有当某条路由的目录确实携带 `peak` 档位时才会渲染该标注——这也正是让这个标签成为一种证据的原因：产品中出现 `(peak)` 或 `(off-peak)` 标注，就证明部署自己的设置提供了那条规则；它不出现，也就证明设置没有提供。

两个视图读取的都是模型选择器自己的按会话目录（`ctx.modelDirectories`），而不是第二个目录，因此在设置中修改的费率无需刷新即可为下一轮定价；没有挂载该插件的部署会得到一个稳定的空数据源，从而不渲染任何金额。由于阅读对话的人可能从不打开选择器，每个视图会自行加载该目录；让它闲置就会什么都定不了价。

**只加载了一部分的轮次不报告任何金额，而 composer 停靠区会向前回溯历史把它补全。** 从一轮中间开始的窗口既不持有该轮的提示词，也不持有它的全部步骤，因此其合计无从得知；报告这个碎片会低估读者正在查看的那一轮。于是 composer 停靠区的统计行会在用量药丸旁边提供一个 `Load turn N` 控件，目标是窗口尚未完整持有的、接下来的那一轮：当窗口是从某一轮中间开始时，就是窗口头部所处的那一轮——必须先补完它才能再往前；否则就是窗口第一轮之前紧邻的那一轮。反复使用它就会一次一轮地向后回溯整个会话，标签始终写明下一次点击会取回哪一轮；而当没有更多历史时 `hasMore` 会把它隐藏。它经由该轮的 `turn/start` seq 分页，而循环是在该轮的提示词与各步骤*之前*记录这个 seq 的，因此一次点击就会把整轮取回：从读者的消息一直到模型回复的结尾。它挂在停靠区而不是轮次轨道上，因为轨道的悬浮卡片是读者最后才会去看的地方——控件必须待在阅读用量的地方。这也使它位于 Chat 视图之外，因此该停靠区的注册从 Session 绑定注入 `loadThrough`，而不是继承视图的 prop。

**要定位那一轮需要两个事实，而其中一个此前并不存在。** 候选者来自宿主机整份日志的 `turnOutline` 投影，但判断窗口是否完整持有某一轮，需要窗口中最旧的*事件*的 seq：某一轮的 `turn/start` 早于它第一个可见节点，因此把该节点的锚点与该轮起点相比，无法区分「窗口正好从这一轮开始」和「窗口从这一轮中间开始」——每一轮看起来都未被覆盖，这正是该控件起初卡在最新一轮、永远无法回溯到它之前那些轮次的原因。现在由 `SessionSnapshot.baseSeq` 暴露这个 seq，而精确的判定是 `baseSeq <= turn/start`。

模型选择器与 composer 座位中的每一行还以 `$in / $out` 显示费率，这正是让顺序变得可读的原因：部署的 `models` 列表顺序就是选择器的顺序。两个入口都经由 `rates.ts` 中同一个共享的 `formatRate` 渲染，因此同一条路由不会在两个菜单里出现两种写法。它至少打印两位小数：把末尾的零一路裁掉会把线上的 `$0.60` 输出费率变成 `$0.6`，一眼看去像是另一个数字，因此只有整数才去掉小数，低于一分的费率则保留其精度。当该路由公布了缓存命中费率时，它也会出现在同一个单元格里，因为在长时间的 agent 对话中大多数提示 token 都是缓存读取，决定账单的是那个费率而不是标称的一对价格；没有公布该费率的路由只显示那一对价格，而不是显示一个谁都不该拿来相乘的 `$0.00`。

## Alternatives considered

**只在 `PiAiModelProfile` 上加价格字段。** 那样部署可以声明费率，但金额仍然没有通向任何界面的路径；`LlmModelCost` 这道接缝正是让同一个折叠服务 Chat、Trajectory 以及此后任何界面的东西。

**从 pi-ai 目录为网关模型推导费率。** `opencode-go` 的目录把 `deepseek-v4-flash` 标为 `$0.22/$0.66`，那样无需一行配置就能为每个自动刷新的模型显示金额。它被否决了：价格属于实际计费的端点，而不属于描述另一个端点的厂商目录，而且内置目录会过期——2026-09-10 DeepSeek 下调 Flash 费率后，该条目仍在按 `$0.22/$0.66` 为一条 CommandCode 与 OpenCode Go 线上费率表都写着 `$0.15/$0.60` 的路由计价，于是推导出的金额会高出五成，却依然显得权威。目录价格仍然是目录模型的来源，那才是它权威的地方。

**把峰时档声明成第二张价格表。** 每个模型多四个数字，每个都可能与它的基础档孪生值发生偏移，而费率表一旦改了比例就要逐个单元格修改。一个系数就声明了公布的规则，基础档也仍是价格唯一被书写的地方。

**从浏览器时钟而不是该次尝试的时刻读取档位。** 更简单，却会悄悄改写历史：同一轮在周二读出一个价格、在周三读出双倍，而同一会话的两位读者对它的花费各执一词。

**对任何跨过边界的轮次都不给出金额。** 安全，却会让本部署最大会话中 226 个带有尝试记录的轮次里的 5 个金额留空——恰恰是读者最想看到价格的长轮次。

**把缺失的一半费率按零计费。** 方便，但恰好在要紧的方向上出错：每个请求都会低估花费，而那个数字看起来还很权威。

**把缺失的费率默认为零并总是渲染金额。** 那样未定价的路由会被读成免费——这是花费界面绝不能给出的那一个错误答案。

**用第一条路由的费率为跨路由轮次报告总额。** 该轮的桶并未按路由拆分，因此这样算出的总额没有描述这一轮；界面选择什么都不显示，读者便看得出没有单一费率适用。

## Verification

- `pnpm exec vitest run packages/llm/token-meter packages/llm/llm packages/llm/llm-pi-ai packages/api/session-controller packages/client/ui-chat packages/client/ui-trajectory packages/client/ui-model-selection`
- `usage-cost.spec.ts` 覆盖缩放求和、不足百万的情形、跳过空桶、无法定价桶的拒绝，以及空读数；`rate-schedule.spec.ts` 覆盖时间窗两端与排他的结束端、同一时钟时刻在工作日属峰时而周末不属峰时的规则、只声明其他星期的时间窗、六种无法定位的时间窗（全部闭合到基础档）、无法定位的时刻，以及缩放后的各档；`turn-cost.client.spec.ts` 覆盖单路由轮次、缓存桶、峰时档尝试、跨过边界因而同时使用两档的轮次、中途换模型并按路由分别定价、未定价路由、未记录路由、缺失的按次尝试记录，以及缺少某个已计费桶的费率。
- `model-cost.spec.ts` 覆盖声明的档位、施加到目录费率上的档位、空块视为未声明档位，以及七种被拒绝的档位；`service.spec.ts` 覆盖核心自身对档位的解绑校验，以及它对无法定位的档位的拒绝；`turn-usage.spec.ts` 覆盖每次尝试的桶、路由与结束时刻，包括由重试结束的一次尝试和提供方从未归属的一次尝试。
- `model-cost.spec.ts` 经由真实 profile 解析器解析声明的费率、为目录模型读取目录费率、为未定价的网关模型报告无费率，并拒绝只声明一半的价对。
- `turn-usage-panel.client.spec.tsx` 与 `table.client.spec.tsx` 在两个界面断言渲染出的金额及其缺失。
- `rates.client.spec.ts` 锁定该单元格的格式：整数费率保留两位小数，低于一分的费率保留精度，整数去掉小数，浮点噪声在四位处舍入，而缺失的缓存费率会被省略而不是渲染成 `$0.00`。
- `chat-stats.client.spec.tsx` 覆盖该加载控件：它会补全窗口从中间进入的那一轮，在窗口正好从某一轮起点开始时向前回溯一轮，经由目标轮的 `turn/start` seq 分页，在分页已尽时隐藏，在加载中显示忙碌文案，并在唯一可显示的内容就是那一轮未完成轮次时仍保持统计行存活。
- 在 [llm-streaming.zh.md](../../../../docs/subsystems/llm-streaming.zh.md) 上记录 `LlmModelCost` 之后运行 `pnpm run verify-type-equiv`。

## Consequences

展示花费的表层如今依赖“费率在声明路由处一并声明”。两个网关的模型端点都不发布价格，因此没有 `cost` 块的路由只渲染 token、不渲染金额：想要展示花费的部署需要自行转录并维护这份费率，而未定价的路由会明确显示为未定价，而不是静默地当作免费。由于每次尝试都带有其结束时刻，费率调整只改设置、不改历史——已记录的轮次保留它运行时所处的时段，而修改后的费率无需重新加载即可为下一轮计价。金额始终是公开标价而非账单记录：无法放置的时段会保守回落到基础费率，只声明一半的价目对会在写入处被拒绝，而没有任何单一费率能描述的轮次则完全不报金额。只记录基础档的上下文分档价目卡会让这些估算成为下限。
